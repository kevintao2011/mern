// import { createProxyMiddleware } from 'http-proxy-middleware';

// const apiProxy = createProxyMiddleware('/api', { target: 'http://localhost:3001', changeOrigin: false });
// // const authProxy = createProxyMiddleware('/auth', { target: 'http://localhost:3002', changeOrigin: true });

// export default function setupProxy(app) {
//   app.use(apiProxy);
// //   app.use(authProxy);
// }